fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5

# LLM provider — install ONE of these depending on which backend you use.
# (Both can coexist; the LLM_PROVIDER env var picks at runtime.)
anthropic>=0.40
openai>=1.40
